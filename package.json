{
  "name": "fora-do-site",
  "private": true,
  "scripts": {
    "setup": "python -m pip install -r backend/requirements.txt",
    "dev": "python backend/run_local.py",
    "start": "python backend/run_local.py"
  }
}